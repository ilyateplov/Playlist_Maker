<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/settings"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp"
        android:gravity="center_vertical">
        <ImageView
            android:id="@+id/bBack"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:padding="12dp"
            android:src="@drawable/ic_back" />
        <TextView
            android:id="@+id/bSearch"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="12dp"
            android:drawablePadding="@dimen/icon_padding"
            android:text="@string/title_settings"
            android:textColor="@color/text"
            android:textSize="@dimen/text_size"
            android:textStyle="bold"/>
    </LinearLayout>

    <TextView
        android:id="@+id/bDarktheme"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="@dimen/item_padding_horizontal"
        android:paddingVertical="@dimen/item_padding_vertical"
        android:drawablePadding="@dimen/icon_padding"
        android:text="@string/item_dark_theme"
        android:textColor="@color/text"
        android:textSize="@dimen/item_text_size"
        app:drawableRightCompat="@drawable/ic_control" />

    <TextView
        android:id="@+id/bShareapp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="@dimen/item_padding_horizontal"
        android:paddingVertical="@dimen/item_padding_vertical"
        android:drawablePadding="@dimen/icon_padding"
        android:text="@string/item_share_app"
        android:textColor="@color/text"
        android:textSize="@dimen/item_text_size"
        app:drawableRightCompat="@drawable/ic_share" />

    <TextView
        android:id="@+id/bWritesupport"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="@dimen/item_padding_horizontal"
        android:paddingVertical="@dimen/item_padding_vertical"
        android:drawablePadding="@dimen/icon_padding"
        android:text="@string/item_write_support"
        android:textColor="@color/text"
        android:textSize="@dimen/item_text_size"
        app:drawableRightCompat="@drawable/ic_support" />

    <TextView
        android:id="@+id/bUseragreement"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="@dimen/item_padding_horizontal"
        android:paddingVertical="@dimen/item_padding_vertical"
        android:drawablePadding="@dimen/icon_padding"
        android:text="@string/item_agreement"
        android:textColor="@color/text"
        android:textSize="@dimen/item_text_size"
        app:drawableRightCompat="@drawable/ic_agreement" />

</LinearLayout>